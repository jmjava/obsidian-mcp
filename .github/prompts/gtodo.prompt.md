---
description: Shortcut for global todo, no prompt
agent: agent
argument-hint: global follow-up
---

Same as `/todo-global`. Call `add_todo` with `scope="global"`. Do not ask. Never persist secrets.
