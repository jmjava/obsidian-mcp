---
description: Remember something after asking repo vs global scope
agent: agent
argument-hint: what not to forget
---

Store a reminder with `add_todo`. If scope was not specified, ask: this GitHub repo, or global? Then save. Shortcuts: `/remember-repo`, `/remember-global`, `/rremember`, `/gremember`. Never persist secrets.
