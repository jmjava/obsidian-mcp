---
description: Shortcut for repo-scoped todo, no prompt
agent: agent
argument-hint: repo follow-up
---

Same as `/todo-repo`. Call `add_todo` with `scope="repo"` and `repository_path` as the workspace root. Do not ask. Never persist secrets.
