---
name: rtodo
description: Shortcut for /todo-repo. Add a repo-scoped Obsidian todo without asking.
---

# /rtodo

Same as `/todo-repo`. Call `add_todo` with `scope="repo"` and `repository_path` set to the workspace root. Do not ask about scope. Never persist secrets.
