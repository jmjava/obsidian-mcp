---
name: gremember
description: Shortcut for /remember-global. Global reminder, no prompt.
---

# /gremember

Same as `/remember-global`. `add_todo` with `scope="global"`. Do not ask. Never persist secrets.
