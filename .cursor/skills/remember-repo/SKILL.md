---
name: remember-repo
description: Remember a repo-scoped follow-up without asking. Use for /remember-repo.
---

# /remember-repo

Call `add_todo` with `scope="repo"` and `repository_path` set to the workspace root. Do not ask about scope. Never persist secrets.
