---
name: gtodo
description: Shortcut for /todo-global. Add a global Obsidian todo without asking.
---

# /gtodo

Same as `/todo-global`. Call `add_todo` with `scope="global"`. Do not ask about scope. Never persist secrets.
