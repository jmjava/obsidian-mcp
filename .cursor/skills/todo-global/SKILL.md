---
name: todo-global
description: Add a global Obsidian todo without asking. Use for /todo-global when the item is not tied to one GitHub repo.
---

# /todo-global

Add an open checkbox with `add_todo` and `scope="global"`. Do not ask about scope.

1. Use the text after the command as the item.
2. Do not pass a project. Write to `AI Memory/Todos.md`.
3. Reply with the vault path. Never persist secrets.
