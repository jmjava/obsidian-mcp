---
name: rremember
description: Shortcut for /remember-repo. Repo-scoped reminder, no prompt.
---

# /rremember

Same as `/remember-repo`. `add_todo` with `scope="repo"`. Do not ask. Never persist secrets.
